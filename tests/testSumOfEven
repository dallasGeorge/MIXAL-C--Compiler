{ 
    var limit : int;
    var sum : int;
    var numb : int;

    limit = 10;
    sum = 0;
    numb = 1;
    
    while (numb <= limit) {
        if (numb % 2 == 0) {
            print(numb);
            sum = sum + numb;
        }
        numb = numb + 1;
    }
    print(sum);
}